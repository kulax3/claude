# Claude Code プロジェクト設定

## 作業スタイル（重要）

### コード編集の進め方
- 実装前に必ず **計画（Plan）を提示して承認を得てから**実行する
- 「まずこのタスクについて考えて、計画を立てて、承認を得てから実行して」が基本指示
- いきなりコードを書き始めない

### Q&Aファースト
- 新機能・新タスクは質問と回答を繰り返して理解を深めてから着手する

## エージェントチームへの作業割り当て

複数のサブタスクが発生した場合は、適切なエージェントに割り振る:
- コードレビュー → `code-reviewer` エージェント
- バグ検証・品質確認 → `code-validator` エージェント
- 字幕・文字起こし校正 → `transcript-checker` エージェント

## 安全操作ルール

- `rm -rf` など削除系コマンドは必ず承認を取ること
- `.env` や認証情報ファイルは絶対に編集・削除しない
- `git push --force` は実行しない

## ショートカット早見表

| 操作 | 効果 |
|------|------|
| `Shift + Tab` | 編集の自動承認モード切替 |
| `Shift + Enter` | 改行（`/terminal-setup` 後に有効） |
| `Esc` | 動作を即停止（セッションは壊れない） |
| `Esc` × 2 | 履歴を遡る |
| `Ctrl + R` | 詳細出力・現在のコンテキスト確認 |
| `#` + メッセージ | CLAUDE.md に自動記録（記憶させたいこと） |
| `!` + コマンド | 一時的にBashモードで実行、結果もコンテキストへ |

## 起動オプション

```bash
claude --model opus --effort high   # 推奨起動オプション
claude --resume                      # 前回セッションを再開
claude --continue                    # 前回セッションを継続
claude --dangerously-skip-permissions  # 自動承認モード（慣れてから）
```

## 初回セットアップ手順（ローカルに戻ったら実行）

→ `setup-guide.md` を参照
