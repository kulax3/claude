# Claude Code プロジェクト設定

## 作業スタイル（重要）

### コード編集の進め方
- 実装前に必ず **計画（Plan）を提示して承認を得てから**実行する
- 「まずこのタスクについて考えて、計画を立てて、承認を得てから実行して」が基本指示
- いきなりコードを書き始めない

### Q&Aファースト
- 新機能・新タスクは質問と回答を繰り返して理解を深めてから着手する

## ショートカット早見表

| 操作 | 効果 |
|------|------|
| `Shift + Tab` | 編集の自動承認モード切替 |
| `Shift + Enter` | 改行（`/terminal-setup` 後に有効） |
| `Esc` | 動作を即停止（セッションは壊れない） |
| `Esc` × 2 | 履歴を遡る |
| `Ctrl + R` | 詳細出力・現在のコンテキスト確認 |
| `#` + メッセージ | CLAUDE.md に自動記録（記憶させたいこと） |
| `!` + コマンド | 一時的にBashモードで実行、結果もコンテキストへ |

## CLI コマンド

```bash
claude --resume    # 前回のセッションを再開
claude --continue  # 前回のセッションを継続
```

## 初回セットアップ手順（ローカルに戻ったら実行）

→ `setup-guide.md` を参照

## コンテキスト管理のコツ

- このファイル（CLAUDE.md）は毎回自動で読み込まれる
- **短く・重要なことだけ**書く（長すぎるとコンテキストを消費）
- 重要なファイルの場所、ルール、コーディングスタイルを記載する
