# ローカルに戻ったら実行するセットアップ手順

Claude Code開発者推奨の初期設定まとめ。**上から順に実行**してください。

## Step 1: ターミナル最適化

```
/terminal-setup
```
→ `Shift + Enter` で改行できるようになる

## Step 2: テーマ設定

```
/theme
```
→ ライト / ダーク / 色覚サポートから選択

## Step 3: ツール許可リストを設定

```
/allowed-tools
```
→ 毎回確認されるツールを事前に許可登録して承認の手間を削減

## Step 4: GitHubアプリのインストール（チーム開発・PR連携）

```
/install-github-app
```
→ ClaudeがPRレビューやコメントを自動で行えるようになる

## Step 5: 設定の確認・調整

```
/config
```
→ モデル選択（Sonnet / Opus）や各種オプションを一括設定

---

## 日常的な使い方のコツ

### 作業開始時の指示テンプレート

```
まずこのタスクについて考えて、計画（Plan）を立てて、
私の承認を得てから実行してください。

タスク: [ここに内容を記載]
```

### セッション再開

```bash
claude --resume    # 前回の会話を再開
claude --continue  # 前回の会話を継続
```

### 記憶させたいことを保存

チャット中に `#` を付けてメッセージを送ると CLAUDE.md に自動記録される。

例：
```
# このプロジェクトはPythonのバージョン3.11を使用すること
```

---

## MCPサーバーの追加（上級）

よく使うCLIツールやサービスをClaudeに教えることで、
それらを活用して作業してくれるようになる。

```bash
# MCPサーバー追加の例
claude mcp add <server-name>
```

## スクリーンショットツールの連携（上級）

自分の出力結果をClaudeに検証させる仕組みを作ると、
Claudeが自分で間違いを発見・修正しながら反復的に精度を上げてくれる。
